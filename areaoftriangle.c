#include<stdio.h>
int main()
{
	float b,h,area;
	printf("program to calculate area of triangle")
	printf("\a enter base and height of  triangle \n : ");
	scanf("%f%f",&b,&h);
	area= 0.5*b*h;
	printf("the area of triangle is %f",area);
	return 0;
}
